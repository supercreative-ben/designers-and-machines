/** Lu.ma embed URL for the Join tab. */
export const LUMA_EMBED_URL: string | null =
  "https://luma.com/embed/event/evt-eiPCn8nUh0jg2CO/simple";

/**
 * Public slug of the next dinner's Lu.ma page (the part after lu.ma/).
 * /api/spots reads the live spots-remaining count from it.
 */
export const LUMA_EVENT_SLUG: string | null = "8vk9mwtp";
